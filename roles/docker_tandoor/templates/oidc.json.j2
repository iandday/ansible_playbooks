{
    "openid_connect": {
        "APPS": [
            {
                "provider_id": "oidc",
                "name": "Authentik",
                "client_id": "{{ vault_docker_tandoor_oidc_client_id }}",
                "secret": "{{ vault_docker_tandoor_oidc_client_secret }}",
                "settings": {
                    "server_url": "{{ vault_docker_tandoor_oidc_issuer_url }}.well-known/openid-configuration"
                }
            }
        ]
    }
}