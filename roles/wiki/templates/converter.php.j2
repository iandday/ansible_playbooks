#<?php die() ?>
# PHP include hack
# {{ ansible_managed }}

#
# Use this file to setup the document to text converter.
#
# The plugin trys to convert every media document to a text file. On this
# progress it uses a given set of external tools to convert it.
# This tools are defined per file extension.
#
# The config stores one extension and it's tool per line.
# You can use %in% and %out% for the input and output file.
#
# example
#
#pdf                            /usr/bin/pdftotext %in% %out%
#doc                            /usr/bin/antiword %in% > %out%

pdf /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
docx /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
doc /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
xlsx /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
xls /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
pptx /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
ppt /usr/bin/java -jar {{ wiki_tika_path }}/tika-app-{{ wiki_tika_version }}.jar --text %in% > %out%
